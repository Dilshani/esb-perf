# Scripts to start remote tomcat backend and WSO2 EI Instances. # 

Place the files in the relevant remote machines. 
Place the ssh config file in the client .ssh/ folder.
Update the ipaddresses accordingly
